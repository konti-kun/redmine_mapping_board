<h3 class="title"><%= l(:label_select_issue) %></h3>

<%= form_for @note, :as => :note, :url => mappingboard_notes_path(:mappingboard_id => @mappingboard), :remote => true, :method => :post do |f| %>
  <%= render :partial => 'new_form' %>
  <p class="buttons">
    <%= submit_tag l(:button_add), :id => 'note-add-submit' %>
    <%= link_to_function l(:button_cancel), "hideModal(this);" %>
  </p>
<% end %>
